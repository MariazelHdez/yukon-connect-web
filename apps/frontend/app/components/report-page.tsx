import type { PageData } from '../lib/strapi';
export function ReportPage({ page }: { page: PageData }) { return <><section className="page-hero"><div className="site-container"><div className="green-block"/><h1>{page.title}</h1>{page.subtitle?<p>{page.subtitle}</p>:null}</div></section><main className="page-content site-container"><RichText text={page.body}/>{page.tableData?<ReportTable data={page.tableData}/>:null}<ReportEmbed url={page.embedUrl}/></main></>; }
function RichText({text}:{text?:string}){return <div className="richtext">{(text??'').split('\n\n').map((b,i)=>{if(b.startsWith('## '))return <h2 key={i}>{b.slice(3)}</h2>; if(b.startsWith('**Tip:**')) return <p className="tip" key={i}>{b.replaceAll('**','')}</p>; return <p key={i}>{b}</p>})}</div>}
export function ReportTable({data}:{data:{columns:string[];rows:string[][]}}){return <div className="report-table-wrap"><table className="report-table"><thead><tr>{data.columns.map(c=><th key={c}>{c}</th>)}</tr></thead><tbody>{data.rows.map((r,i)=><tr key={i}>{r.map((c,j)=><td key={`${i}-${j}`}>{c}</td>)}</tr>)}</tbody></table></div>}
export function ReportEmbed({url}:{url?:string}){return <section className="embed-placeholder">{url?<iframe src={url} title="Embedded report"/>:<><h2>Power BI report embed</h2><p>Publish an embed URL in Strapi to display an interactive report here.</p></>}</section>}
