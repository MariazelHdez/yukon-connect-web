import Link from 'next/link';
import { defaultFooter, type Footer } from '../lib/strapi';
export function SiteFooter({ footer = defaultFooter }: { footer?: Footer }) { const links=footer.navLinks?.length?footer.navLinks:defaultFooter.navLinks; return <footer className="site-footer"><div className="site-container footer-grid"><div><div className="footer-logo">Yukon Connect</div><p className="footer-tagline">Business insight and procurement discovery for Northern entrepreneurs.</p></div><nav className="footer-nav">{links?.map((l)=><div key={l.href}><Link className="footer-heading" href={l.href}>{l.label}</Link>{l.children?.map(c=><Link key={c.href} href={c.href}>{c.label}</Link>)}</div>)}</nav></div><div className="site-container disclaimer">{footer.disclaimer ?? defaultFooter.disclaimer}</div></footer>; }
